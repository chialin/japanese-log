# Favicon Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 新增 `favicon.svg`（棕橘圓角底 + 紙色「あ」書法骨架）並以相對路徑連結注入全部 30 個 HTML。

**Architecture:** 一個靜態 `favicon.svg`（重用 KanjiVG 03042 三條筆畫 path）。2 個 jigen 頁由 `build-jigen-pages.mjs` 樣板提供 favicon link（先改樣板再重跑）；其餘 28 個既有 HTML 由一次性冪等腳本 `scripts/add-favicon.mjs` 注入。沿用專案「一次性 node 腳本 + 純靜態產物」慣例。

**Tech Stack:** 純靜態 HTML/SVG；Node.js ESM（無第三方套件）。

無 build/test framework；「測試」= 指令驗證（檔案數、grep、決定性/冪等重跑）+ 瀏覽器目視。

**關鍵順序：** Task 2（改 build 樣板 + 重跑）必須在 Task 3（add-favicon）之前 —— 這樣 2 個 jigen 頁先帶上 favicon link，add-favicon 掃到它們時因「已含 rel="icon"」而 skip，不會雙重注入。

---

### Task 1: 建立 favicon.svg

**Files:**
- Create: `favicon.svg`

- [ ] **Step 1: 建立 favicon.svg**

三條 path 的 `d` 逐字取自 `assets/kanjivg/03042.svg`（不得改動筆畫資料）。檔頭含 KanjiVG CC-BY-SA 衍生版權註解。

```xml
<?xml version="1.0" encoding="UTF-8"?>
<!--
  favicon.svg — 日本語学習日誌
  「あ」字形衍生自 KanjiVG (03042)，作者 Ulrich Apel。
  原作 (C) 2009-2011 Ulrich Apel，授權 Creative Commons Attribution-Share Alike 3.0。
  本衍生作品同以 CC-BY-SA 3.0 釋出。KanjiVG: http://kanjivg.tagaini.net
  授權條款: http://creativecommons.org/licenses/by-sa/3.0/
-->
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 64 64" width="64" height="64">
  <rect width="64" height="64" rx="12" fill="#c96830"/>
  <g transform="translate(8.2 7.4) scale(0.404)"
     fill="none" stroke="#fdf6f0" stroke-width="15"
     stroke-linecap="round" stroke-linejoin="round">
    <path d="M31.01,33c0.88,0.88,2.75,1.82,5.25,1.75c8.62-0.25,20-2.12,29.5-4.25c1.51-0.34,4.62-0.88,6.62-0.5"/>
    <path d="M49.76,17.62c0.88,1,1.82,3.26,1.38,5.25c-3.75,16.75-6.25,38.13-5.13,53.63c0.41,5.7,1.88,10.88,3.38,13.62"/>
    <path d="M65.63,44.12c0.75,1.12,1.16,4.39,0.5,6.12c-4.62,12.26-11.24,23.76-25.37,35.76c-6.86,5.83-15.88,3.75-16.25-8.38c-0.34-10.87,13.38-23.12,32.38-26.74c12.42-2.37,27,1.38,30.5,12.75c4.05,13.18-3.76,26.37-20.88,30.49"/>
  </g>
</svg>
```

- [ ] **Step 2: 驗證 path 資料與原始 KanjiVG 一致**

Run:
```bash
node -e "const fs=require('fs');const k=fs.readFileSync('assets/kanjivg/03042.svg','utf8');const f=fs.readFileSync('favicon.svg','utf8');const ds=[...k.matchAll(/ d=\"([^\"]+)\"/g)].map(m=>m[1]);const fds=[...f.matchAll(/ d=\"([^\"]+)\"/g)].map(m=>m[1]);console.log('kvg paths',ds.length,'favicon paths',fds.length);console.log('all match', fds.length===3 && fds.every(d=>ds.includes(d)));"
```
Expected:
```
kvg paths 3 favicon paths 3
all match true
```

- [ ] **Step 3: 瀏覽器目視微調**

用瀏覽器（preview 或直接開檔）開啟 `favicon.svg`：確認棕橘 `#c96830` 圓角方底、紙色 `#fdf6f0`「あ」**置中**、四周留白適中、線條粗細勻稱；再縮到 16px 心理檢查仍可辨識為「あ」。
若不置中或太滿/太小，**只調** `<g>` 的 `transform`（translate / scale）與 `stroke-width`，不得改 `d`、不得改顏色、不得改 viewBox。微調後重跑 Step 2 確認 `all match true`。

- [ ] **Step 4: Commit**

```bash
git add favicon.svg
git commit -m "Add favicon.svg (あ, derived from KanjiVG, CC-BY-SA 3.0)"
```

---

### Task 2: build-jigen-pages 樣板加 favicon link + 重跑

**Files:**
- Modify: `scripts/build-jigen-pages.mjs`
- Regenerate: `lessons/2026-05-17-hiragana-jigen.html`, `lessons/2026-05-17-katakana-jigen.html`

- [ ] **Step 1: 在 page() 樣板 `<title>` 後插入 favicon link**

`scripts/build-jigen-pages.mjs` 的 `page()` 回傳的樣板字串中，head 目前為：

```
<title>${kindZh} 字源 × 筆順 — ${kindEn} Origins</title>
<style>
```

用 Edit 把上述兩行改成（在中間插入一行；jigen 頁在 `lessons/` 下故用 `../favicon.svg`）：

```
<title>${kindZh} 字源 × 筆順 — ${kindEn} Origins</title>
<link rel="icon" type="image/svg+xml" href="../favicon.svg" />
<style>
```

即：把模板中 `</title>\n<style>` 替換為 `</title>\n<link rel="icon" type="image/svg+xml" href="../favicon.svg" />\n<style>`。不得改動其他任何部分。

- [ ] **Step 2: 重跑產生器**

Run: `node scripts/build-jigen-pages.mjs`
Expected:
```
wrote lessons/2026-05-17-hiragana-jigen.html
wrote lessons/2026-05-17-katakana-jigen.html
```

- [ ] **Step 3: 驗證兩頁各含且僅含 1 個正確 favicon link，無其他漂移**

Run:
```bash
grep -c '<link rel="icon" type="image/svg+xml" href="../favicon.svg" />' lessons/2026-05-17-hiragana-jigen.html lessons/2026-05-17-katakana-jigen.html
git diff --stat
grep -c 'class="jg-romaji"' lessons/2026-05-17-hiragana-jigen.html
```
Expected: 兩檔各 `1`；`git diff --stat` 只列 `scripts/build-jigen-pages.mjs` 與兩個 jigen html（無其他檔）；`jg-romaji` 仍 `46`（內容未回歸）。

- [ ] **Step 4: 決定性確認**

Run: `node scripts/build-jigen-pages.mjs && git status --porcelain`
Expected: `git status --porcelain` 只顯示已改的 3 個檔（重跑不再產生額外 diff）。

- [ ] **Step 5: Commit**

```bash
git add scripts/build-jigen-pages.mjs lessons/2026-05-17-hiragana-jigen.html lessons/2026-05-17-katakana-jigen.html
git commit -m "Add favicon link to jigen page template"
```

---

### Task 3: add-favicon 注入腳本 + 注入 28 個既有 HTML

**Files:**
- Create: `scripts/add-favicon.mjs`
- Modify (注入 +1 行): root `*.html`（`credits.html` `index.html` `my-name-katakana.html` `slowpaper.html`）、`lessons/*.html`（含 `_skeleton.html`，2 個 jigen 頁會被 skip）、`readings/*.html`

- [ ] **Step 1: 建立 scripts/add-favicon.mjs**

```javascript
#!/usr/bin/env node
// scripts/add-favicon.mjs
//
// 冪等注入 favicon link 到全站 HTML 的 <head>（緊接 <title> 行之後）。
// GitHub Pages 為專案頁，不能依賴根網域 /favicon.ico，故用相對路徑：
//   根目錄頁 → favicon.svg ；lessons/ 與 readings/ → ../favicon.svg
// 已含 rel="icon" 的檔會 skip（jigen 頁由 build 樣板提供，會在此 skip）。
//
// 用法： node scripts/add-favicon.mjs   （從 repo root 執行）

import { readdir, readFile, writeFile } from 'node:fs/promises';

const ICON_RE = /rel=["']icon["']/;

function linkLine(indent, prefix) {
  return `${indent}<link rel="icon" type="image/svg+xml" href="${prefix}favicon.svg" />`;
}

async function htmlFilesIn(dir) {
  const entries = await readdir(dir);
  return entries
    .filter(n => n.endsWith('.html'))
    .map(n => (dir === '.' ? n : `${dir}/${n}`));
}

const files = [
  ...(await htmlFilesIn('.')),
  ...(await htmlFilesIn('lessons')),
  ...(await htmlFilesIn('readings')),
].sort();

let injected = 0, skipped = 0;
for (const file of files) {
  const src = await readFile(file, 'utf8');
  if (ICON_RE.test(src)) { skipped++; console.log(`skip ${file}`); continue; }

  const lines = src.split('\n');
  const idx = lines.findIndex(l => l.includes('</title>'));
  if (idx === -1) {
    console.error(`✗ ${file} — no </title> line, left untouched`);
    skipped++;
    continue;
  }
  const titleLine = lines[idx];
  const indent = titleLine.slice(0, titleLine.length - titleLine.trimStart().length);
  const prefix = file.includes('/') ? '../' : '';
  lines.splice(idx + 1, 0, linkLine(indent, prefix));
  await writeFile(file, lines.join('\n'), 'utf8');
  injected++;
  console.log(`+ ${file}`);
}
console.log(`\ndone: ${injected} injected, ${skipped} skipped`);
```

- [ ] **Step 2: 執行注入**

Run: `node scripts/add-favicon.mjs`
Expected: 結尾 `done: 28 injected, 2 skipped`（被 skip 的 2 個即兩個 jigen 頁，輸出含 `skip lessons/2026-05-17-hiragana-jigen.html` 與 `skip lessons/2026-05-17-katakana-jigen.html`）。

- [ ] **Step 3: 驗證全站覆蓋與相對路徑正確**

Run:
```bash
grep -rl 'rel="icon"' index.html credits.html my-name-katakana.html slowpaper.html lessons/*.html readings/*.html | wc -l
grep -c 'href="favicon.svg"' index.html
grep -c 'href="../favicon.svg"' lessons/2026-05-04-kana.html readings/2026-05-05-reading-cho.html lessons/_skeleton.html
grep -rc 'rel="icon"' $(grep -rl 'rel="icon"' index.html credits.html my-name-katakana.html slowpaper.html lessons/*.html readings/*.html) | grep -v ':1$' || echo "every file exactly 1 (good)"
```
Expected: 第一行 `30`（全部 30 個 HTML 都有）；`index.html` 有 `1` 個 root 相對 `favicon.svg`；三個子目錄檔各 `1` 個 `../favicon.svg`；最後印 `every file exactly 1 (good)`（無任一檔重複注入）。

- [ ] **Step 4: 冪等確認**

Run: `node scripts/add-favicon.mjs`
Expected: 結尾 `done: 0 injected, 30 skipped`。再跑 `git status --porcelain` —— 不得有任何已追蹤 HTML 出現新變動（除 Step 2 已改的；本次 0 變動）。

- [ ] **Step 5: Commit**

```bash
git add scripts/add-favicon.mjs index.html credits.html my-name-katakana.html slowpaper.html lessons readings
git commit -m "Inject favicon link into all site HTML"
```

---

### Task 4: 最終整體驗證

- [ ] **Step 1: 全站 favicon 覆蓋與路徑**

Run:
```bash
ls favicon.svg
total=$(ls -1 *.html lessons/*.html readings/*.html | wc -l)
have=$(grep -rl 'rel="icon"' *.html lessons/*.html readings/*.html | wc -l)
echo "total=$total have=$have"
for f in $(grep -rl 'rel="icon"' lessons/*.html readings/*.html); do grep -q 'href="../favicon.svg"' "$f" || echo "BAD prefix in $f"; done; echo "subdir-prefix-check done"
for f in credits.html index.html my-name-katakana.html slowpaper.html; do grep -q 'href="favicon.svg"' "$f" || echo "BAD prefix in $f"; done; echo "root-prefix-check done"
git status --porcelain
```
Expected: `favicon.svg` 存在；`total=30 have=30`；只印 `subdir-prefix-check done` 與 `root-prefix-check done`（無 BAD 行）；`git status` 乾淨（全 commit）。

- [ ] **Step 2: 決定性 + 冪等回歸**

Run:
```bash
node scripts/build-jigen-pages.mjs
node scripts/add-favicon.mjs
git status --porcelain
```
Expected: add-favicon 印 `done: 0 injected, 30 skipped`；build 印兩行 wrote；`git status --porcelain` **空**（兩腳本皆無副作用漂移）。

- [ ] **Step 3: 瀏覽器目視**

開 `index.html` 與任一 `lessons/*.html`：瀏覽器分頁出現棕橘「あ」favicon；DevTools Console 無 error；Network 中 favicon 為本地相對請求（非外部、非 404）。`favicon.svg` 單獨開啟仍清晰置中。

- [ ] **Step 4: 對照 spec 收尾**

逐項對照 `docs/superpowers/specs/2026-05-17-favicon-design.md` 的「交付物」與「驗證」清單打勾；如有缺漏回對應 Task 修正。

---

## Self-Review

**1. Spec coverage**

| Spec 要求 | 對應 Task |
|---|---|
| `favicon.svg`：64×64、`#c96830` 圓角底、紙色「あ」、KanjiVG 三 path | Task 1 |
| 檔頭 CC-BY-SA 衍生版權註解 | Task 1 Step 1（註解區塊） |
| 不改 d / 顏色 / viewBox，只可微調 transform | Task 1 Step 3 限制 + Step 2 path 一致性檢查 |
| 注入字串、root vs `../` 規則、緊接 `</title>` | Task 3（`prefix` 由是否含 `/` 決定；splice 在 `</title>` 行後） |
| 冪等（已含 rel="icon" 跳過） | Task 3 Step 1 `ICON_RE` + Step 4 |
| 決定性同步 build 樣板（`../favicon.svg`）並重跑使逐字相同 | Task 2；字串與 Task 3 `linkLine(..,'../')` 逐字一致（`<link rel="icon" type="image/svg+xml" href="../favicon.svg" />`） |
| `_skeleton.html` 也覆蓋（`../favicon.svg`） | Task 3 掃描 `lessons/*.html` 含 `_skeleton.html`，prefix `../` |
| 30 個 HTML 全覆蓋 | Task 3 Step 3 + Task 4 Step 1（total=30 have=30） |
| 執行順序：先 build 樣板+重跑，再 add-favicon | 計畫頭「關鍵順序」+ Task 2 在 Task 3 之前 |
| 不改 shared.css / 其他內容 / 不做 ICO-PNG | 無相關 Task；Task 2/3 限定 diff 範圍 |

無缺漏。

**2. Placeholder scan:** 無 TBD/TODO；favicon.svg、add-favicon.mjs、build 樣板編輯、所有驗證指令與 commit 皆為完整可執行內容。Task 1 Step 3 的 transform 微調有明確邊界（只動 transform/stroke-width，且須重過 path 一致性檢查），非含糊佔位。

**3. Type/字串一致性:** 注入連結字串在 Task 2（build 樣板，`../favicon.svg`）與 Task 3（`linkLine`，子目錄亦 `../favicon.svg`）逐字相同：`<link rel="icon" type="image/svg+xml" href="../favicon.svg" />`，故 jigen 頁被 add-favicon 的 `ICON_RE` 命中而 skip、git 不漂移。檔案清單（4 root + 25 lessons + 1 reading = 30）與各驗證步驟一致。`htmlFilesIn`/`ICON_RE`/`linkLine` 命名一致，無孤立引用。
